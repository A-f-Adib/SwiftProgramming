
struct  QueueArray {

    private var arr = [Int]()

    var isEmpty : Bool {
        return arr.isEmpty
    }

    var peek : Int? {
        return arr.first
    }

    init() {
        arr[]
    }
}